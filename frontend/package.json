{
  "name": "cursos-flix-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",                     // Versão consolidada do React
    "react-dom": "^18.2.0",                 // Para renderização em DOM
    "react-router-dom": "^6.11.2",          // Roteamento no React
    "axios": "^1.6.2",                      // Requisições HTTP (API calls)
    "tailwindcss": "^3.3.7",                // Framework CSS utilitário para estilização
    "postcss": "^8.4.27",                   // Processamento do CSS
    "autoprefixer": "^10.4.15",             // Adiciona prefixes automaticamente ao CSS
    "react-scripts": "5.0.1",               // Scripts padrão do Create React App
    "eslint": "^8.42.0",                    // Linting para garantir qualidade do código
    "eslint-config-react-app": "7.0.1",     // Configuração padrão do eslint para apps React
    "@testing-library/react": "^14.1.1",    // Ferramenta para testar componentes React
    "@testing-library/jest-dom": "^5.16.5",   // Extensões para o Jest com DOM assertions
    "@testing-library/user-event": "^14.4.3", // Simulação de interações do usuário
    "jest": "^29.6.4"                       // Framework de testes (para garantir consistência com o backend)
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
